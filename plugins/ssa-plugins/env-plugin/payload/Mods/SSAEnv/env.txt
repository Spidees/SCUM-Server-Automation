# Environment tuning. All values are MULTIPLIERS of the game's defaults. Re-read live.
drying_multiplier = 3.0          # >1 = things dry faster
wetting_multiplier = 0.5         # <1 = get wet slower (water immersion + wet surfaces)
rain_multiplier = 0.5            # <1 = get wet slower from RAIN
dirtiness_multiplier = 0.3       # <1 = get dirty slower; 0 = never get dirty from surfaces
surface_wetness_multiplier = 1.0 # <1 = surfaces make you wet less (on top of wetting_multiplier)
debug = 0                        # 1 = print debug lines to the UE4SS log; 0 = silent
